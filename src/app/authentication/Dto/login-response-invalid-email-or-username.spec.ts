import { LoginResponseInvalidEmailOrUsername } from './login-response-invalid-email-or-username';

describe('LoginResponseInvalidEmailOrUsername', () => {
  it('should create an instance', () => {
    expect(new LoginResponseInvalidEmailOrUsername()).toBeTruthy();
  });
});
