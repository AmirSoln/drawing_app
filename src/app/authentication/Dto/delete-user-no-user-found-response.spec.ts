import { DeleteUserNoUserFoundResponse } from './delete-user-no-user-found-response';

describe('DeleteUserNoUserFoundResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteUserNoUserFoundResponse()).toBeTruthy();
  });
});
