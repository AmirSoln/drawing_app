import { DeleteUserResponseOk } from './delete-user-response-ok';

describe('DeleteUserResponseOk', () => {
  it('should create an instance', () => {
    expect(new DeleteUserResponseOk()).toBeTruthy();
  });
});
