import { EditMarkerRequest } from './edit-marker-request';

describe('EditMarkerRequest', () => {
  it('should create an instance', () => {
    expect(new EditMarkerRequest()).toBeTruthy();
  });
});
