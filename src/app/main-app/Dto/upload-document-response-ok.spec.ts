import { UploadDocumentResponseOk } from './upload-document-response-ok';

describe('UploadDocumentResponseOk', () => {
  it('should create an instance', () => {
    expect(new UploadDocumentResponseOk()).toBeTruthy();
  });
});
